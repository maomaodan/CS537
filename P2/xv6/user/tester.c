// Do not modify this file. It will be replaced by the grading scripts
// when checking your project.

#include "types.h"
#include "stat.h"
#include "user.h"
#include "pstat.h"

int
main(int argc, char *argv[])
{
/*		test for pstat pointer
struct pstat st;
int i = 0;
getpinfo(&st);
  printf(1, "%s", "tester output\n");
for (i = 0; i < 10; i++)
printf(1, "pid: %d inuse: %d\n", st.pid[i],st.inuse[i]);
*/
  printf(1, "%s", "** Placeholder program for grading scripts **\n");
  exit();
}
